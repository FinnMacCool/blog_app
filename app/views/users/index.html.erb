<% provide(:title, "Liste der User") %>
<h1>Liste der User</h1>

<div class="content">
  <ul>
    <%= render @users %>
  </ul>
  <% if signed_in? && current_user.admin? %>
    <br/>
    <div align="center">
      <%= link_to "Neuer User", new_user_path %>
    </div>
  <% end %>
</div>